language: go

services:
  - docker

go:
  - "1.10.x"
  - master

before_install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh

install:
  - dep ensure

script: go test -race -v ./...
